# Sunucu tarafında (Route Handler'lar) kullanılır, tarayıcıya asla gönderilmez.
# Production'da Hostinger hPanel → (frontend sitesi) → Ortam değişkenleri'nden
# verilir; deploy'lar bu değerlere dokunmaz. Durum kontrolü: /api/health
#
# ZORUNLU — "http(s)://" ile başlamalı ve "/api" ile bitmeli (sondaki "/" sorun değil).
LARAVEL_API_URL=http://127.0.0.1:8123/api
# OPSİYONEL — verilmezse LARAVEL_API_URL'den "/api" çıkarılarak türetilir.
LARAVEL_BASE_URL=http://127.0.0.1:8123

# Tarayıcıda Laravel Echo/Reverb bağlantısı için — bunlar public bilgilerdir
# (Reverb app key'i zaten WebSocket handshake'inde açık şekilde kullanılır).
# Production'da (Hostinger) EKLEMEYİN: tanımsız olunca frontend polling'e düşer.
NEXT_PUBLIC_REVERB_APP_KEY=
NEXT_PUBLIC_REVERB_HOST=127.0.0.1
NEXT_PUBLIC_REVERB_PORT=8080
NEXT_PUBLIC_REVERB_SCHEME=http
